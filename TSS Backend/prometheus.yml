global:
  scrape_interval: 15s  # Set the scrape interval (default: 15s)

scrape_configs:
  - job_name: 'nodejs-app'
    metrics_path: /metrics   # Ensure your Node.js app exposes metrics at /metrics
    static_configs:
      - targets: ['backend.jouls.co.in']  # Your live server IP & port



# do docer compose up 